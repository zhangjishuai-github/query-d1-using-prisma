{
  "name": "query-d1-using-prisma",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "predeploy": "wrangler d1 migrations apply DB --remote",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "prisma": "^6.8.2",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.18.0"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^6.8.2",
    "@prisma/client": "^6.8.2"
  }
}
